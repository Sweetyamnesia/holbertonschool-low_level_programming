#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
* main - Entry point
* Description : The main function will be called without any parameters.
*Return: Always 0 (Success)
*/

int main(void)
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
{
putchar(ch);
}
{
putchar ('\n');
}
for (ch = 'A'; ch <= 'Z'; ch++)
{
putchar(ch);
}
return (0);
}
