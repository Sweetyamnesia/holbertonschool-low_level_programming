#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - Entry point
* Description : The main function will be called without any parameters.
*Return: Always 0 (Success)
*/

int main(void)
{
for (int = '0'; int <= '9'; int++)
{
putchar(int);
}
putchar('\n');
return (0);
}
