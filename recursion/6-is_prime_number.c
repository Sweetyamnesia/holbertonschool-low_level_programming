#include "main.h"
#include <stdio.h>

/**
* is_prime_number - Function that returns a prime number.
* @n: The integer.
* Return: 1 if the input integer is a prime number, 0 if it doesn't.
*/

int is_prime_number(int n)
{
if (n > 1)
{
return (1);
}
else
{
return (0);
}
}
