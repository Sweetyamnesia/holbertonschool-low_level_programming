#include <string.h>
#include <stdio.h>
#include "main.h"

/**
* print_rev - function that prints a string in reverse.
* @s: the parameter.
**/

void print_rev(char *s)
{
int i;
char *s;  
_putchar(*s);
}
