#include <stdio.h>
/**
* print_to_98 - function that prints all numbers from n to 98.
* @n: number.
**/

void print_to_98(int n)
{
for (int i = n; i <= 98; i++)
{
printf("%d", i);
if (i < 98)
{
printf(", ");
}
}
printf("\n");
}
